<UserControl x:Class="ProgrammTrackerBody.Views.LogsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{DynamicResource Logs.Packet}" FontWeight="SemiBold" Margin="0,0,0,4" />
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Packets}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Logs.Time}" Binding="{Binding Time}" Width="100" />
                    <DataGridTextColumn Header="{DynamicResource Logs.Direction}" Binding="{Binding Direction}" Width="80" />
                    <DataGridTextColumn Header="{DynamicResource Logs.Endpoint}" Binding="{Binding Endpoint}" Width="160" />
                    <DataGridTextColumn Header="{DynamicResource Logs.Type}" Binding="{Binding PacketType}" Width="140" />
                    <DataGridTextColumn Header="{DynamicResource Logs.Summary}" Binding="{Binding Summary}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{DynamicResource Logs.System}" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBox Grid.Row="1"
                     Text="{Binding SystemLog, Mode=OneWay}"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"
                     FontFamily="Consolas" />
        </Grid>
    </Grid>
</UserControl>
